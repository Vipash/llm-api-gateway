global:
  scrape_interval: 5s # Set short interval for real-time dashboard updates

scrape_configs:
  - job_name: "fastapi-llm-gateway"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["host.docker.internal:8000"]